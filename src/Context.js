import React from 'react';

const ClientContext = React.createContext(null);
const CatalogusContext = React.createContext(null);
const ZaaktypeContext = React.createContext(null);

export { ClientContext, CatalogusContext, ZaaktypeContext };
